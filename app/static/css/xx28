.model-picker select {
    border: 1px solid var(--color-border);
    border-radius: var(--radius-sm);
    padding: 5px 10px;
    font-size: 0.82rem;
    background: #fff;
    cursor: pointer;
}

/* ----- API Settings ----- */
