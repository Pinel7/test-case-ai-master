.batch-edit-dialog .footer-actions {
    display: flex; gap: 8px; justify-content: flex-end;
    margin-top: 20px; padding-top: 12px;
    border-top: 1px solid var(--color-border);
}
[data-theme="dark"] .batch-edit-dialog { background: #23242e; }
[data-theme="dark"] .batch-edit-overlay { background: rgba(0,0,0,.6); }

/* ============================
   REGEX TESTER
   ============================ */
